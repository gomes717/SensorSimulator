# TODO

- [ ] Adicionar cálculo das métricas clínicas de CGM (TIR, TAR, TBR, GMI, CV, Mean Glucose) sobre as curvas "expected" e "received", tanto no app quanto na documentação de análise de resultados.
- [ ] Implementar simulação de evento PISA (Pressure-Induced Sensor Attenuation) nos modelos de sensor — atenuação transitória de corrente por deslocamento/pressão do eletrodo, gerando falso valor baixo de glicose sem refletir hipoglicemia real.
- [ ] Permitir que o MCU funcione a partir de um CSV em vez do modelo fisiológico: o app envia o CSV pela BLE, o firmware o armazena (flash externa) e passa a transmitir uma linha por tick como CGM Measurement, em vez de rodar o `model_thread`. O firmware também deve consumir as refeições do Food Log CSV correspondente (carboidratos por horário). Os botões "Insert Food Now" / "Insert Exercise Now" continuam na janela principal e continuam funcionando por cima da reprodução do CSV. No app já existe a escolha de fonte de dados por paciente (modelo vs região CSV) em `PersonProfile.data_source` / `csv_path` / `csv_window_start_iso` e a janela CSV Analysis para escolher o arquivo e a janela de 24 h; falta a reprodução em si (no app e no MCU) e o transporte BLE do CSV.
- [ ] Implementar o PISA como evento injetável do modelo de sensor (além do item acima): atenuação transitória descendente da corrente por pressão/deslocamento do eletrodo produzindo um falso baixo sem hipoglicemia real; expor como evento "inserir agora" no app (semelhante a food/exercise instantâneos) e sombrear o intervalo afetado no gráfico.
- [ ] Substituir o "Fast mode" fixo (1 s = 1 sim-minuto) por um multiplicador de velocidade contínuo de x1 a x1000: slider na janela de Configuração, aplicado ao `dt_min` do `SimulationEngine` do app e propagado para o tick do MCU. Hoje só existem os dois estados (normal e fast); o objetivo é um fator ajustável.
